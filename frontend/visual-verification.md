# Visual verification notes

The desktop canvas renders with an original calm botanical knowledge-workspace direction: warm off-white canvas, deep garden-green brand mark, editorial serif headings, grouped pastel regions, directional card links, and a focused right-side inspector. The authenticated preview showed the canvas rather than a generic landing page, with source-linked cards and a visible AI suggestion affordance.

The first capture caught the auth loading state, so a short auth-settlement fallback was added to ensure unauthenticated users reach the welcome screen instead of remaining on a loader if session discovery is delayed. The second desktop capture verified the main product shell at 1280x720. A mobile breakpoint is implemented in CSS by collapsing the inspector into a slide-over panel and compressing the rail, but it still needs a mobile screenshot capture.
